# Environment
.env
.env.*
!.env.example

# OS / Windows metadata
.DS_Store
Thumbs.db
*:Zone.Identifier

# Node
node_modules/
.npm/
.vite/
dist/
build/

# PHP / Laravel
vendor/
tuts-core/storage/framework/cache/*
tuts-core/storage/framework/sessions/*
tuts-core/storage/framework/views/*
tuts-core/storage/logs/*
tuts-core/storage/app/public/pdfs/
!tuts-core/storage/framework/cache/.gitignore
!tuts-core/storage/framework/sessions/.gitignore
!tuts-core/storage/framework/views/.gitignore
!tuts-core/storage/logs/.gitignore

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Local RAG indexes; preserve locally, do not publish without explicit review.
tuts-rag-service/faiss_db/

# Local tools / secrets
credentials.json
token.json
service_account.json

# Temporary
*.tmp
*.log

# Local handoff/support artifacts kept outside the repository.
/Arquitetura Técnica do Projeto TUTS.pdf
/tuts-rag-faiss-fix-handoff.tar.gz

# Nested workspace copies/artifacts; keep local unless reviewed explicitly.
tuts-core/tuts-core/
tuts-core/tuts-rag-service/
tuts-core/tuts-scrapers/
tuts-core/tree_tuts_core.txt
